import java.util.Arrays;

public class App {
    public static int sum(int[] numbers)
  {
    Arrays.sort(numbers);
    if (numbers.length > 1){
        
    }
    return 0;
  }
    public static void main(String[] args) {
        
    }
}